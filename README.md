## **Participantes**
**juandonoso@liceovvh.cl**
**alexanderpino@liceovvh.cl**

# Listado de funciones 

# Ejercicio N°1:
registroPagos() se encarga de seleccionar todo el codigo (parte 1) para que funcione todo lo principal.

pagoPermiso() se encarga de la condición que da instrucciones al primer código para que logre funcionar.



# Ejercicio N°2: 
agregarLista() se encarga de agregar el nombre seleccionado en el input con .push para que se agregue al final del arreglo.

agregarUrgencia() se encarga de agregar el nombre seleccionado en el input con .unshift para que se agregue al inicio del arreglo.

listaVacunacion1() se encarga de seleccionar todo el codigo de JS (parte 1), para que este pueda funcionar (incluye las condicionales).

listaVacunacion2() se encarga de seleccionar todo el codigo de JS (parte2), para que este pueda funcionar (incluye las condicionales).

## Ejercicio N°3:
buscarBeneficio() se encarga de las instrucciones para que el bucle busque los datos que seleccionará el input dada la información en el arreglo.

verificarBeneficio() se encarga de todo el proceso principal para que funcione el bucle y todo el resto de código importante para la funcionalidad de este.